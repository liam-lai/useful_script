<source>
  @type tail
  path # full path
  tag tag123-1
  <parse>
    @type none
  </parse>
</source>

<source>
  @type tail
  path # full path
  tag tag123-2 
  <parse>
    @type none
  </parse>
</source>

<match tag123*>
    @type forward
    send_timeout 5s
    <server>
      host full.ip
      port 24224
    </server>
</match>
