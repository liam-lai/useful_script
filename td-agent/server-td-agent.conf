<match devnet*>
  @type file
  path /var/log/${tag}
  append true
  <buffer tag,time>
    @type file
    flush_mode immediate
    path /var/log/fluentd_buffer
  </buffer>
  <format>
    @type single_value
  </format>
</match>

<source>
  @type forward
  @id input_forward
  port 24224
</source>
